# The API the app talks to. On a real phone or tablet "localhost" is the
# device itself — use your machine's LAN IP (the same network the device is
# on), e.g. http://192.168.1.20:8888
EXPO_PUBLIC_API_BASE_URL=http://localhost:8888
